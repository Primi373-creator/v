{
  "name": "image-server",
  "version": "1.0.0",
  "description": "Express server for storing and serving images from MongoDB",
  "main": "server.js", // Change this if your server file has a different name
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.13.0",
    "multer": "^1.4.2"
  }
}
